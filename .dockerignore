 # .dockerignore
    .git
    node_modules
    .next
    npm-debug.log*
    yarn-debug.log*
    pnpm-debug.log*
    yarn-error.log*
    .env*
    !.env.production # Include production env if needed at build time (usually not)
    .DS_Store
    Dockerfile
    docker-compose.yml # We don't need this inside the image
    README.md

    